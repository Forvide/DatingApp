{
    "version": "0.2.0",
    "configurations": [
        {
            "name": ".NET Core Attach",
            "type": "coreclr",
            "request": "attach"
        },

        {
            "name": "Attach to Chrome",
            "request": "launch",
            "type": "pwa-chrome",
            "url": "https://localhost:4200",
            "webRoot": "${workspaceFolder}"
        }
    ]
}