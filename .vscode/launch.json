{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to Chrome",
            "request": "launch",
            "type": "pwa-chrome",
            "url": "https://localhost:4200",
            "webRoot": "${workspaceFolder}"
        }
    ]
}